name: publisher

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */3 * * *"

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install boto3 requests

      - name: Run Mode B
        env:
          RUN_MODE: B

          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
          BUCKET_NAME: ${{ secrets.BUCKET_NAME }}
          R2_PUBLIC_BASE_URL: ${{ secrets.R2_PUBLIC_BASE_URL }}

          B_PREFIX_PRIORITY: ugc/final_priority
          B_PREFIX_MANUAL: ugc/final_manual
          B_PREFIX_AUTO: ugc/final
          B_META_FINAL_PREFIX: ugc/meta/final
          B_STATE_KEY: ugc/state/mode_b_state.json
          B_MAX_PUBLISH_PER_RUN: "20"

          # filtro temporal para evitar backlog viejo
          B_ONLY_KEYS_CONTAIN: "2026-03-09"
          B_AVOID_SAME_SOURCE_PER_RUN: "true"

          ENABLE_INSTAGRAM: "true"
          ENABLE_FACEBOOK: "true"
          ENABLE_TIKTOK: "false"
          ENABLE_SHORTS: "false"
          DRY_RUN: "false"

          GRAPH_VERSION: "v25.0"
          HTTP_TIMEOUT: "30"

          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}

          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}

        run: |
          python -u main.py
